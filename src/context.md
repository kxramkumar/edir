i need model_deploy.py should have below things
1. i will add the model location and name of it in a list
2. its an streamlit application
3. layout should be it should be on top and bottom
4. top it host to get the folder directory / file directory
5. bottom it should list all the modle list; in array as one tab
6. in a single tab; is assigned to a model
7. for each file in a folder, it shoudl show a row with all the attributes against the model
8. when i selected the row it has to show piccture location belowthe original picture and the enchanced picuture which used by the model to predict in the top section; at that time the upload should toggle to view mode
9. first it has to lsit all the files in rows with out the result attribute filed
10. when i clicke tht tab it has to start/resumt test for each file in that path for that mode and shows the result
11. first it has to lsit all the files in rows with out the result attribute filed.. on execution completed of a file it hast o updated against that row
12. the result of each file has to be cached untiel we change the path the test path