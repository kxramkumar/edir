"""Eye disease classification app. Extend as needed."""


def run():
    """Simple entry point. Extend with model load, inference, etc."""
    print("Eye disease classification app — run()")


if __name__ == "__main__":
    run()
